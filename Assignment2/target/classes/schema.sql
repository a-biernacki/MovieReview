CREATE TABLE movie (
	id LONG PRIMARY KEY AUTO_INCREMENT NOT NULL,
	name VARCHAR(255) NOT NULL,
	rating VARCHAR(255) NOT NULL,
	genre VARCHAR(255) NOT NULL,
	platform VARCHAR(255) NOT NULL,
	reviewDate date,
	reviewTime VARCHAR(225) NOT NULL
);
